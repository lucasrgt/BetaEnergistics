# External Worldline specs

These Java 8 specifications consume the packaged Worldline TestKit. They test
BetaEnergistics as an external project and never compile Worldline sources.
Run `java tools/testkit/Run.java` with `WORLDLINE_TESTKIT_HOME` configured.
